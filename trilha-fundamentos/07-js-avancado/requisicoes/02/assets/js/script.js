// Request = Requisição (req)
// Quando você acessa um site, você está enviando uma requisição para te enviarem o arquivo do site.

// Headers = Cabeçalhos
// Vai junto com a req com algumas informações do usuário

// Body = Corpo
// A mensagem que o usuário envia ao fazer a requisição(Ás vezes está vazio por não ser necessário, tipo entrar no Google)


// Response = Resposta (res)
// A resposta enviada a requisição

// Headers
// Onde é enviado informações internas, de cache normalmente (que não aparecem)

// Body
// Normalmente o conteúdo (Como o HTML da página, por exemplo)
